% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{fastIntersect}
\alias{fastIntersect}
\title{Fast Intersection Between Two Vectors}
\usage{
fastIntersect(x, y, keep.duplicated = TRUE)
}
\arguments{
\item{x, y}{Vectors (of the same mode) containing a sequence of items.}

\item{keep.duplicated}{Logical. If \code{TRUE}, duplicates in \code{x} are preserved.
Otherwise, duplicates are removed before matching.}
}
\value{
A vector of elements from \code{x} that are also in \code{y}. The result
preserves the order in \code{y}. Returns \code{NULL} if either input is \code{NULL}.
}
\description{
Performs a fast intersection between two vectors, with optional control
over whether to preserve duplicated elements in the first input.
}
\examples{
fastIntersect(c("a", "b", "b", "c"), c("b", "c", "d"))
# [1] "b" "b" "c"

fastIntersect(c("b", "b", "c"), c("b", "c", "d"), keep.duplicated = FALSE)
# [1] "b" "c"

fastIntersect(NULL, c("a", "b"))
# NULL

}
